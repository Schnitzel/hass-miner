# Extends the official Home Assistant devcontainer image.
# The upstream Dockerfile (home-assistant/devcontainer/apps/Dockerfile) requires
# files from the devcontainer repo's common/ directory, so we consume the
# pre-built image instead.
FROM ghcr.io/home-assistant/devcontainer:4-apps
