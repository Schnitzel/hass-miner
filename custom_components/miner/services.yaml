reboot:
  description: Choose miner device to reboot
  target:
    entity:
      integration: miner

restart_backend:
  description: Choose miner device to restart its backend
  target:
    entity:
      integration: miner
